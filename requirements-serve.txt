# runtime for the scoring service (install the CPU torch wheel first on servers: pip install torch --index-url https://download.pytorch.org/whl/cpu)
numpy>=1.26
pandas>=2.1
pyyaml>=6
torch>=2.2
fastapi>=0.110
uvicorn>=0.29
